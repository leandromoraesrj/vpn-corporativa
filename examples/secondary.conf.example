# Configuração opcional da VPN secundária.
# Copie para ~/.config/vpn/secondary.conf e substitua pelos valores autorizados.
# Rotas opcionais, repetíveis:
# route = valor
# route = valor
portal-url =
# Interface de túnel configurada (o padrão da aplicação é tun0):
interface = tun0
